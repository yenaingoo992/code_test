<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DailyVita" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">
            @color/primary
        </item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.DailyVita" parent="Base.Theme.DailyVita" />

    <style name="Theme.DailyVita.Button" parent="ThemeOverlay.Material3.Button">
        <item name="android:backgroundTint">@color/secondary</item>
    </style>

    <style name="Theme.DailyVita.Headline" parent="TextAppearance.Material3.HeadlineMedium">
        <item name="android:textColor">@color/accent</item>
    </style>

    <style name="Theme.DailyVita.TitleLarge" parent="TextAppearance.Material3.TitleLarge">
        <item name="android:textColor">@color/accent</item>
        <item name="android:textSize">24sp</item>
    </style>

    <style name="Theme.DailyVita.TitleMedium" parent="TextAppearance.Material3.TitleMedium">
        <item name="android:textColor">@color/accent</item>
        <item name="android:textSize">20sp</item>
    </style>
</resources>